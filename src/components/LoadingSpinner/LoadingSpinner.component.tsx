import React from 'react';
import './LoadingSpinner.scss';

interface Props {}

const LoadingSpinner = (props: Props) => {
	return <div className='circleLoading' />;
};

export default LoadingSpinner;
